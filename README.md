# AI-ассистент по анализу переписок

Полнофункциональный MVP веб-приложения для анализа переписок с использованием AI. Сервис анализирует диалоги, определяет тональность сообщений, выявляет проблемные места и предлагает варианты ответов.

## 🎯 Возможности

- **Анализ переписок**: Глубокий анализ диалогов с определением тональности каждого сообщения
- **Варианты ответов**: Готовые формулировки (мягкий, прямой, с юмором, с границами)
- **Выявление проблем**: Автоматическое определение конфликтных моментов
- **Авторизация через VK**: Простая регистрация без email/пароля
- **Тарифные планы**: Free, Pro, Ultra с различными лимитами
- **История анализов**: Сохранение всех проведённых анализов

## 🏗️ Архитектура

### Frontend
- **React 18** + **Vite** — современный стек для быстрой разработки
- **TailwindCSS** — утилитарный CSS-фреймворк
- **React Router** — маршрутизация
- **Axios** — HTTP-клиент
- Mobile-first адаптивный дизайн

### Backend
- **PHP 8.2+** — серверный язык
- **Slim Framework 4** — минималистичный роутер
- **MySQL** — реляционная БД
- **JWT** — токены авторизации
- **Cloud.ru AI API** — анализ через Qwen3-235B-A22B-Instruct-2507

### База данных
- `users` — пользователи (VK ID, имя, аватар)
- `subscriptions` — подписки (план, статус, срок действия)
- `analyses` — история анализов
- `usage_limits` — счётчики использования по месяцам
- `payments` — платежи

## 📊 Юнит-экономика

### Расчёт себестоимости

**Средняя цена анализа через Cloud.ru:**
- Qwen3-235B-A22B-Instruct-2507: зависит от тарифа Cloud.ru (обычно ~0.2–0.5 ₽ за анализ)

### Тарифы и маржинальность

#### FREE (Бесплатный)
- **Лимит**: 5 анализов/месяц
- **Себестоимость**: до 2 ₽/месяц
- **ARPU**: 0 ₽
- **Цель**: Привлечение пользователей, демонстрация возможностей

#### PRO (299 ₽/месяц)
- **Лимит**: 100 анализов/месяц
- **Себестоимость**: до 24 ₽/месяц (при использовании всех 100)
- **ARPU**: 299 ₽
- **Маржа**: ~275 ₽ (92%)
- **LTV**: 2.5–3 месяца = ~750 ₽

#### ULTRA (499 ₽/месяц)
- **Лимит**: 500 анализов/месяц
- **Себестоимость**: ~60 ₽/месяц (при среднем использовании 150 анализов)
- **ARPU**: 499 ₽
- **Маржа**: ~439 ₽ (88%)
- **LTV**: 2.5–3 месяца = ~1250 ₽

### Выводы

- Высокая маржинальность на всех платных тарифах (88–92%)
- Низкая себестоимость FREE-тарифа позволяет масштабировать привлечение
- LTV в 2.5–3 месяца обеспечивает быструю окупаемость маркетинга
- Модель подписки стабилизирует денежный поток

## 🚀 Быстрый старт

### Требования

- PHP 8.2+
- MySQL 8.0+
- Node.js 20+
- Composer
- Nginx (или другой веб-сервер)

### Установка

1. **Клонировать репозиторий**
```bash
git clone <repository-url>
cd LoveCode-1
```

2. **Настроить базу данных**
```bash
mysql -u root -p < db/schema.sql
```

3. **Настроить backend**
```bash
cd backend
cp .env.example .env
# Отредактировать .env файл
composer install
```

4. **Настроить frontend**
```bash
cd ../frontend
npm install
cp .env.example .env
# Отредактировать .env файл (VITE_API_URL)
```

5. **Настроить Nginx**

Пример конфигурации:
```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /var/www/flirt-ai/frontend/dist;
    index index.html;

    # Frontend
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Backend API
    location /api {
        try_files $uri $uri/ /backend/public/index.php?$query_string;
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root/backend/public/index.php;
        include fastcgi_params;
    }
}
```

6. **Запустить разработку**

Backend (через встроенный сервер PHP):
```bash
cd backend
php -S localhost:8080 -t public
```

Frontend:
```bash
cd frontend
npm run dev
```

## ⚙️ Конфигурация

### Переменные окружения (Backend)

**Важно**: Перед настройкой `.env` файла, настройте Cloud.ru AI API (см. [CLOUDRU_SETUP.md](./CLOUDRU_SETUP.md))

Создайте файл `backend/.env`:

```env
# Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=flirt_ai
DB_USER=root
DB_PASSWORD=your_password

# VK OAuth
VK_APP_ID=your_vk_app_id
VK_APP_SECRET=your_vk_app_secret
VK_REDIRECT_URI=https://your-domain.com/api/auth/vk-callback

# JWT
JWT_SECRET=your-secret-key-min-32-chars

# Cloud.ru AI
CLOUDRU_API_KEY=your_cloudru_api_key
CLOUDRU_FOLDER_ID=your_folder_id
CLOUDRU_MODEL=qwen3-235b-a22b-instruct-2507

# YooMoney
YOOMONEY_RECEIVER=410011234567890
YOOMONEY_SECRET=your_secret

# Frontend URL
FRONTEND_URL=https://your-domain.com
```

### Переменные окружения (Frontend)

Создайте файл `frontend/.env`:

```env
VITE_API_URL=https://your-domain.com
```

## 🔐 Настройка VK OAuth

1. Перейдите на [VK Developers](https://dev.vk.com/)
2. Создайте новое приложение (тип: "Веб-сайт")
3. Получите `App ID` и `App Secret`
4. Укажите Redirect URI: `https://your-domain.com/api/auth/vk-callback`
5. Добавьте в `.env` backend

## 💳 Настройка оплаты (ЮMoney P2P)

### Важно
ЮMoney P2P используется как временная схема для MVP без регистрации ИП. Для продакшена рекомендуется перейти на CloudPayments или Telegram Stars.

### Инструкция

1. **Создать кошелёк ЮMoney**
   - Зарегистрируйтесь на [yoomoney.ru](https://yoomoney.ru)
   - Получите номер кошелька (формат: 410011234567890)

2. **Настроить приём платежей**
   - В настройках кошелька включите "Приём платежей"
   - Скопируйте номер кошелька в `YOOMONEY_RECEIVER`

3. **Создание платёжной ссылки**
   - Backend автоматически генерирует ссылку на оплату
   - Формат: `https://yoomoney.ru/quickpay/confirm?receiver=...&sum=...&label=...`

4. **Обработка успешной оплаты**
   - После оплаты пользователь возвращается на `FRONTEND_URL/payment/success?payment_id=...`
   - Backend проверяет статус через `/api/payment/verify/{payment_id}`
   - При успехе активируется подписка на 1 месяц

### Альтернативы для продакшена

- **CloudPayments "Самозанятый"**: Требует регистрации как НПД
- **Telegram Stars**: Если планируется интеграция Telegram MiniApp

## 📁 Структура проекта

```
.
├── backend/                 # PHP Backend
│   ├── public/             # Точка входа (index.php)
│   ├── src/                # Исходный код
│   │   ├── Controllers/    # Контроллеры API
│   │   ├── Models/         # Модели БД
│   │   ├── Services/        # Сервисы (VK, OpenAI, JWT, YooMoney)
│   │   ├── Middleware/     # Middleware (Auth)
│   │   └── routes.php      # Маршруты
│   ├── config/             # Конфигурация
│   ├── composer.json       # PHP зависимости
│   └── .env.example        # Пример переменных окружения
│
├── frontend/               # React Frontend
│   ├── src/
│   │   ├── pages/          # Страницы
│   │   ├── components/     # Компоненты
│   │   ├── contexts/       # React Context (Auth)
│   │   └── App.jsx         # Главный компонент
│   ├── package.json        # Node зависимости
│   └── vite.config.js      # Конфигурация Vite
│
├── db/
│   └── schema.sql          # Схема БД
│
├── .github/workflows/      # CI/CD
│   ├── build.yml           # Сборка и линтинг
│   └── deploy.yml          # Деплой на сервер
│
└── README.md               # Документация
```

## 🔄 CI/CD

### GitHub Actions

#### Build (`build.yml`)
- Проверка кода при push/PR
- Сборка frontend
- Линтинг PHP (PSR-12)

#### Deploy (`deploy.yml`)
- Автоматический деплой при push в `main`
- Требует secrets:
  - `SERVER_HOST` — IP/домен сервера
  - `SERVER_USER` — пользователь SSH
  - `SERVER_PATH` — путь на сервере
  - `SSH_KEY` — приватный SSH-ключ

### Настройка Secrets

1. Перейдите в Settings → Secrets and variables → Actions
2. Добавьте необходимые secrets
3. При push в `main` запустится автоматический деплой

## 🧪 API Endpoints

### Авторизация
- `POST /api/auth/vk-init` — Получить URL для авторизации VK
- `GET /api/auth/vk-callback` — Callback от VK (редирект)

### Анализ (требует авторизации)
- `POST /api/analyze-dialog` — Проанализировать переписку
  ```json
  {
    "text": "Текст переписки..."
  }
  ```

### Профиль (требует авторизации)
- `GET /api/profile` — Получить профиль пользователя
- `GET /api/history` — История анализов (query: `limit`, `offset`)

### Платежи (требует авторизации)
- `POST /api/payment/create-session` — Создать платёжную сессию
  ```json
  {
    "plan": "pro" | "ultra"
  }
  ```
- `GET /api/payment/verify/{payment_id}` — Проверить статус платежа
- `POST /api/payment/webhook` — Webhook от платёжной системы

## 🎨 Дизайн

### Принципы
- Минимализм и простота
- Светлая тема
- Скругления 8px
- Адаптивность (mobile-first)

### Цветовая схема тональности
- **Нейтральный** — серый (`#6b7280`)
- **Тёплый** — зелёный (`#10b981`)
- **Холодный** — голубой (`#3b82f6`)
- **Раздражение** — красный (`#ef4444`)
- **Тревожность** — янтарный (`#f59e0b`)

## 📝 Тарифы

### Free
- 5 анализов в месяц
- Базовый анализ
- 2 варианта ответов
- История ограничена

### Pro (299 ₽/месяц)
- 100 анализов в месяц
- Все варианты ответов (4)
- Приоритетная обработка
- Полная история

### Ultra (499 ₽/месяц)
- 500 анализов в месяц
- Улучшенный тональный анализ
- AI-коуч по коммуникации
- Приоритетная поддержка

## 🐛 Troubleshooting

### Проблемы с авторизацией VK
- Проверьте `VK_REDIRECT_URI` в настройках приложения VK
- Убедитесь, что домен совпадает

### Ошибки OpenAI API
- Проверьте `OPENAI_API_KEY` в `.env`
- Убедитесь, что на балансе есть средства

### Проблемы с платежами
- Проверьте номер кошелька ЮMoney
- Убедитесь, что включён приём платежей

## 📄 Лицензия

MIT

## 👥 Контакты

Для вопросов и предложений создайте issue в репозитории.
