# 🔄 Инструкция по обновлению проекта (OpenAI → Cloud.ru)

## ⚠️ Важно: Резервное копирование

Перед обновлением создайте резервную копию:

```bash
# На сервере
cd /var/www/flirt-ai
tar -czf backup-$(date +%Y%m%d-%H%M%S).tar.gz backend/ frontend/ db/
```

---

## 📋 Что изменилось

### Новые файлы:
- `backend/src/Services/CloudRuService.php` — новый сервис для Cloud.ru
- `CLOUDRU_SETUP.md` — инструкция по настройке Cloud.ru

### Изменённые файлы:
- `backend/src/Controllers/AnalysisController.php` — использует CloudRuService
- `backend/config/config.php` — секция `openai` заменена на `cloudru`
- `README.md` — обновлена документация
- `DEPLOY.md` — обновлены переменные окружения

### Удаляемые файлы:
- `backend/src/Services/OpenAIService.php` — больше не нужен (можно удалить после проверки)

---

## 🔧 Шаг 1: Обновление на GitHub

### Вариант A: Если вы работаете один (рекомендуется)

```bash
# На вашем локальном компьютере
cd C:\Users\sp1r1\.cursor\LoveCode\LoveCode-1

# Проверьте текущие изменения
git status

# Добавьте все изменения
git add .

# Закоммитьте изменения
git commit -m "Migrate from OpenAI to Cloud.ru AI API"

# Отправьте на GitHub
git push origin main
```

### Вариант B: Если есть конфликты

```bash
# Получите последние изменения
git pull origin main

# Если есть конфликты, разрешите их вручную
# Затем:
git add .
git commit -m "Merge: Migrate to Cloud.ru"
git push origin main
```

---

## 🖥️ Шаг 2: Обновление на сервере

### 2.1 Обновление через GitHub Actions (автоматически)

Если у вас настроен GitHub Actions, он автоматически задеплоит изменения после push в `main`.

**Проверьте:**
1. Перейдите в GitHub → Actions
2. Дождитесь завершения workflow "Deploy"
3. Проверьте логи на ошибки

### 2.2 Ручное обновление (если нужно)

```bash
# Подключитесь к серверу
ssh root@YOUR_SERVER_IP

# Перейдите в директорию проекта
cd /var/www/flirt-ai

# Создайте резервную копию текущего .env
cp backend/.env backend/.env.backup

# Получите последние изменения
git pull origin main

# Если есть конфликты в .env, разрешите их:
# nano backend/.env
# (сохраните ваши старые настройки БД, VK, JWT, YooMoney)
# (добавьте новые настройки Cloud.ru)
```

---

## ⚙️ Шаг 3: Обновление .env файла

### 3.1 На сервере

```bash
cd /var/www/flirt-ai/backend
nano .env
```

### 3.2 Удалите старые строки OpenAI:

```env
# УДАЛИТЬ ЭТИ СТРОКИ:
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
```

### 3.3 Добавьте новые строки Cloud.ru:

```env
# ДОБАВИТЬ ЭТИ СТРОКИ:
CLOUDRU_API_KEY=your_cloudru_api_key
CLOUDRU_FOLDER_ID=your_folder_id
CLOUDRU_MODEL=qwen3-235b-a22b-instruct-2507
```

**Важно**: Получите эти значения из Cloud.ru (см. [CLOUDRU_SETUP.md](./CLOUDRU_SETUP.md))

### 3.4 Сохраните файл

`Ctrl+O`, `Enter`, `Ctrl+X`

---

## 🗑️ Шаг 4: Удаление старого файла (опционально)

После проверки что всё работает, можно удалить старый файл:

```bash
# На сервере
cd /var/www/flirt-ai/backend/src/Services
rm OpenAIService.php  # Если файл ещё существует

# Или через git (рекомендуется)
cd /var/www/flirt-ai
git rm backend/src/Services/OpenAIService.php
git commit -m "Remove old OpenAI service"
git push origin main
```

---

## ✅ Шаг 5: Проверка работоспособности

### 5.1 Проверка конфигурации

```bash
# На сервере
cd /var/www/flirt-ai/backend
php -r "
require 'vendor/autoload.php';
\$dotenv = Dotenv\Dotenv::createImmutable(__DIR__);
\$dotenv->load();
\$config = require 'config/config.php';
echo 'Cloud.ru config:' . PHP_EOL;
print_r(\$config['cloudru']);
"
```

Должны увидеть ваши настройки Cloud.ru.

### 5.2 Проверка синтаксиса PHP

```bash
php -l backend/src/Services/CloudRuService.php
php -l backend/src/Controllers/AnalysisController.php
php -l backend/config/config.php
```

Не должно быть ошибок.

### 5.3 Перезапуск сервисов

```bash
# Перезапустите PHP-FPM
systemctl restart php8.2-fpm

# Перезагрузите Nginx
systemctl reload nginx
```

### 5.4 Тестовый запрос

1. Откройте сайт: `https://YOUR_DOMAIN.com`
2. Авторизуйтесь через VK
3. Попробуйте проанализировать тестовую переписку
4. Проверьте логи на ошибки:

```bash
# Логи PHP
tail -f /var/log/php8.2-fpm.log

# Логи Nginx
tail -f /var/log/nginx/flirt-ai-error.log
```

---

## 🐛 Решение проблем

### Проблема: "Class CloudRuService not found"

**Решение:**
```bash
# Переустановите зависимости
cd /var/www/flirt-ai/backend
composer dump-autoload
```

### Проблема: "Undefined index: cloudru"

**Решение:**
- Проверьте, что в `backend/config/config.php` есть секция `cloudru`
- Проверьте, что в `.env` файле есть все переменные `CLOUDRU_*`

### Проблема: "Cloud.ru API error"

**Решение:**
- Проверьте правильность `CLOUDRU_API_KEY`
- Проверьте правильность `CLOUDRU_FOLDER_ID`
- Убедитесь, что сервисный аккаунт имеет права `ai.languageModels.user`
- Проверьте доступность модели в вашем регионе

### Проблема: Старый код всё ещё работает

**Решение:**
```bash
# Очистите кеш OPcache (если используется)
php -r "opcache_reset();"

# Перезапустите PHP-FPM
systemctl restart php8.2-fpm
```

---

## 📝 Чек-лист обновления

- [ ] Резервная копия создана
- [ ] Изменения закоммичены в Git
- [ ] Изменения отправлены на GitHub (git push)
- [ ] GitHub Actions деплой завершён успешно (или ручное обновление)
- [ ] Старые переменные `OPENAI_*` удалены из `.env`
- [ ] Новые переменные `CLOUDRU_*` добавлены в `.env`
- [ ] Cloud.ru API настроен (см. CLOUDRU_SETUP.md)
- [ ] Конфигурация проверена (php -r ...)
- [ ] Синтаксис PHP проверен
- [ ] Сервисы перезапущены
- [ ] Тестовый анализ выполнен успешно
- [ ] Логи проверены на ошибки

---

## 🔄 Откат изменений (если что-то пошло не так)

Если нужно вернуться к OpenAI:

```bash
# На сервере
cd /var/www/flirt-ai

# Восстановите из резервной копии
tar -xzf backup-YYYYMMDD-HHMMSS.tar.gz

# Или откатите через git
git log  # Найдите коммит до изменений
git reset --hard COMMIT_HASH

# Восстановите старый .env
cp backend/.env.backup backend/.env

# Перезапустите сервисы
systemctl restart php8.2-fpm
systemctl reload nginx
```

---

## 📞 Дополнительная помощь

- Инструкция по настройке Cloud.ru: [CLOUDRU_SETUP.md](./CLOUDRU_SETUP.md)
- Полная документация: [README.md](./README.md)
- Инструкция по деплою: [DEPLOY.md](./DEPLOY.md)

---

**Готово!** После выполнения всех шагов ваш проект будет использовать Cloud.ru AI вместо OpenAI.

