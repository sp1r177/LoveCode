# Конфигурация Amvera для PHP приложения
# Этот проект использует Dockerfile для сборки

meta:
  environment: docker
  # Dockerfile должен находиться в корне проекта

# Порт, который будет слушать приложение
run:
  type: docker
  port: 80



